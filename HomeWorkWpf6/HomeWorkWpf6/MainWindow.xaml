<Window x:Class="HomeWorkWpf6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HomeWorkWpf6"
        mc:Ignorable="d"
        Title="MainWindow" Height="278.986" Width="349.337">
    <DataGrid x:Name="dataGrid" Height="174" VerticalAlignment="Top" Margin="0,0,2,0">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Id" Binding="{Binding Path=Id}" Width="100" />
            <DataGridTextColumn Header="Employee" Binding="{Binding Path=Login}" Width="120" />
            <DataGridTextColumn Header="Divison" Binding="{Binding Path=Password}" Width="120" />
        </DataGrid.Columns>
    </DataGrid>

</Window>
